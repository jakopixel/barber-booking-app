# Barber Booking App

Monorepo minimale:

- `backend/` Node.js + Express + Prisma + PostgreSQL
- `mobile/` React Native (Expo)

## Avvio rapido

1. Configura `backend/.env`
2. `cd backend && npm i && npx prisma migrate dev`
3. `cd mobile && npm i && npx expo start`

